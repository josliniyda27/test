apiVersion: v1
kind: Service
metadata:
  name: LoadBalancer-service
spec:
  selector:
    app: sample-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
