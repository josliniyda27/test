name: BatchFileWorkflow
on:
  pull_request:
  
jobs: 
  Run_BAT_File:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Release directory
        run: .\\PullDockerImages.bat
      









